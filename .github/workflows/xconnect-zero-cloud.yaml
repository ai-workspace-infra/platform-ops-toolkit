name: XConnect Zero Cloud Lab
on:
  workflow_dispatch:
    inputs:
      deployment_profile:
        description: Deployment profile; disposable AWS Spot cloud lab or persistent existing UAT One
        type: choice
        options: [cloud-lab, existing-one]
        required: false
        default: cloud-lab
      mode:
        type: choice
        options: [dry-run, apply, cleanup]
        default: dry-run
      iac_ref:
        description: Advanced override - reviewed iac_modules commit SHA
        type: string
        required: false
        default: '2ba98f30c3e4199e01eb940bb1951549fca0ae3e'
      gitops_ref:
        description: Advanced override - reviewed gitops commit SHA
        type: string
        required: false
        default: 'fbf78214efdc718802ffc9d170b457fa07fed8f0'
      playbooks_ref:
        description: Advanced override - reviewed playbooks commit SHA
        type: string
        required: false
        default: 'dae04558c6c3d86a9a12158cca6ef3b2fc9bc43f'
      cli_release_tag:
        description: Advanced override - XConnect-One GitHub Release tag
        type: string
        required: false
        default: 'v0.1.14'
      gateway_release_tag:
        description: Advanced override - XConnect-Gateway GitHub Release tag
        type: string
        required: false
        default: 'v0.1.10'
      xray_release_tag:
        description: Advanced override - XTLS/Xray-core Release tag
        type: string
        required: false
        default: 'v26.3.27'
      gateway_provider:
        description: Gateway mode; external reuses the persistent TW Gateway from Vault
        type: choice
        options: [external, aws-spot]
        required: false
        default: external
      external_gateway_id:
        description: Stable Gateway identity for the external non-IaC node
        type: string
        required: false
        default: 'gw-uat-tw-xconnect'
      external_network_id:
        description: Stable Zero network identity for the external Gateway
        type: string
        required: false
        default: 'net_uat'
      external_gateway_server_name:
        description: TLS SNI name for the external Gateway
        type: string
        required: false
        default: 'tw-xconnect.svc.plus'
      network_id:
        description: Existing UAT Zero network identity for the existing-one profile
        type: string
        required: false
        default: 'net_uat'
      one_vault_key:
        description: Existing UAT One Vault record key under uat/ulighthost-xconnect
        type: string
        required: false
        default: 'observability.svc.plus'
      gateway_vault_key:
        description: Stable UAT Gateway Vault record key under uat/ulighthost-xconnect
        type: string
        required: false
        default: 'ph-xconnect.svc.plus'
      gateway_wireguard_address:
        description: Optional Gateway WireGuard IPv4 /32 override inside the GitOps overlay CIDR
        type: string
        required: false
        default: ''
      allow_release_overrides:
        description: Explicitly allow this run to use One/Gateway tags different from the GitOps declaration
        type: boolean
        required: false
        default: false
      cleanup_run:
        description: Cleanup only - exact xcl-RUN_ID-ATTEMPT from a previous run
        type: string
        default: ''
      ssh_debug_ingress_cidrs:
        description: Optional temporary operator SSH IPv4 /32 list, comma-separated; defaults to the current operator egress and is not stored in GitOps
        type: string
        required: false
        default: '35.79.83.48/32'
      gateway_transport_ingress_cidrs:
        description: Temporary controlled-node IPv4 /32 allowlist for the public Gateway VLESS/XHTTP TCP 443 entrypoint; not stored in GitOps
        type: string
        required: false
        default: '35.79.83.48/32'
      matrix_node_filter:
        description: Node filter for the enrollment matrix (none, all, ai-aggregator, or specific node id)
        type: string
        required: false
        default: 'all'
permissions:
  contents: read
  id-token: write
env:
  XCONNECT_VAULT_ROLE: github-actions-platform-ops-toolkit-uat-xconnect-cloud-lab
concurrency:
  group: xconnect-cloud-lab
  cancel-in-progress: false
jobs:
  preflight:
    name: Preflight / ${{ matrix.suite }}
    if: github.event_name == 'workflow_dispatch'
    strategy:
      fail-fast: false
      matrix:
        suite:
          - contracts
          - runtime
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Validate workflow and Vault contracts
        if: matrix.suite == 'contracts'
        run: |
          bash .github/scripts/tests/xconnect_formal_uat_lab_contract_test.sh
          bash .github/scripts/tests/xconnect_cloud_lab_vault_role_contract_test.sh
          bash .github/scripts/tests/xconnect_gateway_dns_contract_test.sh
          bash .github/scripts/tests/xconnect_xhttp_runtime_contract_test.sh
          bash .github/scripts/tests/xconnect_ai_aggregator_matrix_contract_test.sh
      - name: Validate runtime scripts and workflow inputs
        if: matrix.suite == 'runtime'
        run: |
          python3 -m unittest discover -s .github/scripts/xconnect-lab -p 'test_*.py'
          bash .github/scripts/tests/workflow_dispatch_input_limit_test.sh
          bash -n .github/scripts/xconnect-lab/run.sh
          bash -n .github/scripts/xconnect-lab/deploy.sh
          bash -n .github/scripts/xconnect-lab/gateway.sh
          bash -n .github/scripts/xconnect-lab/verify-xhttp-runtime.sh
          bash -n .github/scripts/xconnect-lab/enroll-node.sh
          bash -n .github/scripts/xconnect-existing-one-uat/deploy.sh
          if [[ "${{ inputs.deployment_profile }}" == 'existing-one' && "${{ inputs.mode }}" == 'cleanup' ]]; then
            echo 'cleanup is only supported by the cloud-lab profile' >&2
            exit 1
          fi

  resolve_ai_aggregator_matrix:
    name: Resolve AI Aggregator node matrix
    needs: preflight
    if: github.event_name == 'workflow_dispatch' && inputs.deployment_profile == 'cloud-lab' && inputs.mode == 'apply'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install matrix parser
        run: python3 -m pip install --disable-pip-version-check 'PyYAML==6.0.2'
      - name: Build matrix from GitOps nodes
        id: matrix
        env:
          GITOPS_MANIFEST: gitops/topology/uat/selfhost/ai-aggregator.yaml
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path
          import yaml

          data = yaml.safe_load(Path(os.environ['GITOPS_MANIFEST']).read_text())
          spec = data['spec']
          metadata = data.get('metadata', {})
          nodes = []
          for node in spec.get('nodes', []):
              if node.get('role') not in {'gateway', 'cpa'}:
                  continue
              xconnect = node.get('xconnect', {})
              if xconnect.get('enabled', True) is False:
                  continue
              overlay_ip = xconnect.get('overlay_ip')
              if not overlay_ip:
                  raise SystemExit(f"node {node.get('id')} is missing xconnect.overlay_ip")
              nodes.append({
                  'id': node['id'],
                  'name': node.get('display_name', f"{metadata.get('name', 'ai-aggregator')}-{node['id']}"),
                  'role': node['role'],
                  'subsystem': metadata.get('name', 'ai-aggregator'),
                  'overlay_ip': overlay_ip,
              })
          if not nodes:
              raise SystemExit('GitOps manifest declares no enabled gateway/cpa XConnect nodes')
          ids = [node['id'] for node in nodes]
          if len(ids) != len(set(ids)):
              raise SystemExit('GitOps XConnect node IDs must be unique')
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write('matrix=' + json.dumps({'node': nodes}, separators=(',', ':')) + '\n')
          print('resolved_nodes=' + ','.join(ids))
          PY

  apply:
    name: UAT lab / ${{ inputs.mode }}
    needs: preflight
    if: github.event_name == 'workflow_dispatch' && inputs.deployment_profile == 'cloud-lab' && inputs.mode != 'cleanup'
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      MODE: ${{ inputs.mode }}
      IAC_REF: ${{ inputs.iac_ref || '2ba98f30c3e4199e01eb940bb1951549fca0ae3e' }}
      GITOPS_REF: ${{ inputs.gitops_ref || 'fbf78214efdc718802ffc9d170b457fa07fed8f0' }}
      PLAYBOOKS_REF: ${{ inputs.playbooks_ref || 'dae04558c6c3d86a9a12158cca6ef3b2fc9bc43f' }}
      CLI_RELEASE_TAG: ${{ inputs.cli_release_tag || 'v0.1.14' }}
      GATEWAY_RELEASE_TAG: ${{ inputs.gateway_release_tag || 'v0.1.10' }}
      XRAY_RELEASE_TAG: ${{ inputs.xray_release_tag || 'v26.3.27' }}
      GATEWAY_PROVIDER: ${{ inputs.gateway_provider || 'external' }}
      EXTERNAL_GATEWAY_ID: ${{ inputs.external_gateway_id || 'gw-uat-tw-xconnect' }}
      EXTERNAL_NETWORK_ID: ${{ inputs.external_network_id || 'net_uat' }}
      EXTERNAL_GATEWAY_SERVER_NAME: ${{ inputs.external_gateway_server_name || 'tw-xconnect.svc.plus' }}
      ALLOW_XCONNECT_RELEASE_OVERRIDES: ${{ inputs.allow_release_overrides || false }}
      CLEANUP_RUN: ${{ inputs.cleanup_run }}
      SSH_DEBUG_INGRESS_CIDRS: ${{ inputs.ssh_debug_ingress_cidrs || '35.79.83.48/32' }}
      GATEWAY_TRANSPORT_INGRESS_CIDRS: ${{ inputs.gateway_transport_ingress_cidrs || '35.79.83.48/32' }}
      GATEWAY_WIREGUARD_ADDRESS: ${{ inputs.gateway_wireguard_address }}
      # Apply runs retain the reviewed one-hour observation window. Cleanup is
      # isolated in its own job and uses NODE_OBSERVATION_INPUT=0.
      NODE_OBSERVATION_INPUT: ${{ inputs.mode == 'apply' && 'until-expiry' || '0' }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Validate immutable references
        run: |
          echo "LAB_DIR=$RUNNER_TEMP/xconnect-lab" >> "$GITHUB_ENV"
          LAB_DIR="$RUNNER_TEMP/xconnect-lab" bash .github/scripts/xconnect-lab/run.sh validate
      - name: Authenticate CI to Vault via GitHub JWT
        id: bootstrap
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ env.XCONNECT_VAULT_ROLE }}
          jwtGithubAudience: vault
          exportEnv: false
          secrets: |
            kv/data/CICD/github-app/daily-snapshot app_private_key | APP_PRIVATE_KEY
      - name: Infra repository access
        id: infra-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: Iv23liNwStpQIiXajhpb
          private-key: ${{ steps.bootstrap.outputs.APP_PRIVATE_KEY }}
          owner: ai-workspace-infra
          repositories: iac_modules,gitops,playbooks
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/iac_modules
          ref: ${{ env.IAC_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: iac_modules
          persist-credentials: false
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ env.GITOPS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: gitops
          persist-credentials: false
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ env.PLAYBOOKS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: playbooks
          persist-credentials: false
      - name: Resolve the UAT declaration
        id: topology
        run: bash .github/scripts/xconnect-lab/run.sh topology
      - name: Verify formal Accounts and deployed Portal BFF boundary
        if: inputs.mode != 'cleanup'
        run: python3 .github/scripts/xconnect-lab/probe-control-plane.py gitops/vpn-overlay/uat/xconnect-lab.json
      - name: Private CLI release access
        if: inputs.mode != 'cleanup'
        id: cli-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: Iv23liNwStpQIiXajhpb
          private-key: ${{ steps.bootstrap.outputs.APP_PRIVATE_KEY }}
          owner: ai-workspace-xstream
          repositories: XConnect-One,XConnect-Gateway
      - name: Download and verify project release artifacts
        if: inputs.mode != 'cleanup'
        env:
          CLI_RELEASE_TOKEN: ${{ steps.cli-token.outputs.token }}
          GITHUB_TOKEN: ${{ github.token }}
        run: bash .github/scripts/xconnect-lab/run.sh download
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.10.5
          terraform_wrapper: false
      - name: Offline topology and Terraform preflight
        run: bash .github/scripts/xconnect-lab/run.sh preflight
      - name: Infrastructure credentials from UAT Vault role
        if: inputs.mode != 'dry-run'
        uses: hashicorp/vault-action@v4
        with:
          url: ${{ steps.topology.outputs.vault_address }}
          method: jwt
          role: ${{ steps.topology.outputs.vault_role }}
          jwtGithubAudience: vault
          secrets: |
            kv/data/CICD/uat/iac_state TF_STATE_ENDPOINT | TF_STATE_ENDPOINT ;
            kv/data/CICD/uat/iac_state TF_STATE_BUCKET | TF_STATE_BUCKET ;
            kv/data/CICD/uat/iac_state TF_STATE_ACCESS_KEY | TF_STATE_ACCESS_KEY ;
            kv/data/CICD/uat/iac_state TF_STATE_SECRET_KEY | TF_STATE_SECRET_KEY ;
            kv/data/CICD/uat/iac_state TF_STATE_REGION | TF_STATE_REGION
      - name: Runtime credentials from dedicated Vault path
        if: inputs.mode == 'apply'
        uses: hashicorp/vault-action@v4
        with:
          url: ${{ steps.topology.outputs.vault_address }}
          method: jwt
          role: ${{ steps.topology.outputs.vault_role }}
          jwtGithubAudience: vault
          secrets: |
            kv/data/uat/xconnect-one VLESS_ID | LAB_VLESS_ID ;
            kv/data/uat/xconnect-one ZERO_SERVICE_TOKEN | ZERO_SERVICE_TOKEN ;
            kv/data/uat/xconnect-one ZERO_OWNER_EMAIL | ZERO_OWNER_EMAIL ;
            kv/data/CICD/domains/svc.plus tls_fullchain_pem_b64 | XCONNECT_GATEWAY_TLS_FULLCHAIN_PEM_B64 ;
            kv/data/CICD/domains/svc.plus tls_key_pem_b64 | XCONNECT_GATEWAY_TLS_KEY_PEM_B64 ;
            kv/data/CICD/domains/svc.plus tls_trust_bundle_pem_b64 | XCONNECT_GATEWAY_TLS_TRUST_BUNDLE_PEM_B64 ;
            kv/data/CICD/domains/svc.plus tls_ca_pem_b64 | XCONNECT_GATEWAY_TLS_CA_PEM_B64 ;
            kv/data/prod/ulighthost-xconnect/tw-xconnect.svc.plus host | EXTERNAL_GATEWAY_HOST ;
            kv/data/prod/ulighthost-xconnect/tw-xconnect.svc.plus user | EXTERNAL_GATEWAY_USER ;
            kv/data/prod/ulighthost-xconnect/tw-xconnect.svc.plus ssh_private_key_b64 | EXTERNAL_GATEWAY_SSH_PRIVATE_KEY_B64 ;
            kv/data/CICD/observability user | OBSERVABILITY_USER ;
            kv/data/CICD/observability password | OBSERVABILITY_PASSWORD
      - name: Stable Gateway DNS credentials from UAT Vault
        if: inputs.mode == 'apply' && inputs.gateway_provider == 'external'
        uses: hashicorp/vault-action@v4
        with:
          url: ${{ steps.topology.outputs.vault_address }}
          method: jwt
          role: ${{ steps.topology.outputs.vault_role }}
          jwtGithubAudience: vault
          secrets: |
            kv/data/uat/serverless/cloudflare CLOUDFLARE_ACCOUNT_ID | CLOUDFLARE_ACCOUNT_ID ;
            kv/data/uat/serverless/cloudflare CLOUDFLARE_API_TOKEN | CLOUDFLARE_API_TOKEN
      - name: Reconcile stable Gateway DNS in Cloudflare
        if: inputs.mode == 'apply' && inputs.gateway_provider == 'external'
        env:
          XCONNECT_GATEWAY_DNS_NAME: ${{ env.EXTERNAL_GATEWAY_SERVER_NAME }}
          XCONNECT_GATEWAY_DNS_TARGET: ${{ env.EXTERNAL_GATEWAY_HOST }}
        run: bash .github/scripts/xconnect-lab/reconcile-gateway-dns.sh
      - name: AWS short-lived OIDC credentials
        if: inputs.mode != 'dry-run'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.topology.outputs.aws_role }}
          aws-region: ${{ steps.topology.outputs.aws_region }}
          audience: sts.amazonaws.com
      - name: Prepare isolated state and inspect account prerequisites
        id: prepare
        if: inputs.mode != 'dry-run'
        run: bash .github/scripts/xconnect-lab/run.sh prepare
      - name: Provision the two isolated Spot nodes
        if: inputs.mode == 'apply'
        run: bash .github/scripts/xconnect-lab/run.sh apply
      - name: Install released runtimes and create the Gateway local identity
        if: inputs.mode == 'apply'
        run: bash .github/scripts/xconnect-lab/run.sh setup
      - name: Real Accounts - provision network and one-use Gateway/One invitations
        if: inputs.mode == 'apply'
        run: bash .github/scripts/xconnect-lab/run.sh bootstrap
      - name: Deploy formal Gateway using its invitation and signed config
        if: inputs.mode == 'apply'
        run: bash .github/scripts/xconnect-lab/run.sh gateway
      - name: Join Linux One using its formal invitation and reconcile Gateway
        if: inputs.mode == 'apply'
        run: bash .github/scripts/xconnect-lab/run.sh one
      - name: Verify signed sync/ACK, services, exact-peer handshake and private ping/HTTP
        if: inputs.mode == 'apply'
        run: bash .github/scripts/xconnect-lab/run.sh verify
      - name: Install Ansible controller for XConnect observability
        if: inputs.mode == 'apply'
        run: |
          python3 -m pip install --user 'ansible-core==2.17.7'
          echo "$(python3 -m site --user-base)/bin" >> "$GITHUB_PATH"
      - name: Register Gateway and One base metrics with Observability
        if: inputs.mode == 'apply'
        env:
          OBSERVABILITY_ENDPOINT: ${{ steps.topology.outputs.observability_endpoint }}
          OBSERVABILITY_QUERY_PATH: ${{ steps.topology.outputs.observability_query_path }}
          OBSERVABILITY_ENVIRONMENT: ${{ steps.topology.outputs.observability_environment }}
        run: bash .github/scripts/xconnect-lab/run.sh observability
      - name: Publish non-secret desktop handoff
        if: inputs.mode == 'apply' && inputs.gateway_provider != 'external'
        uses: actions/upload-artifact@v4
        with:
          name: xconnect-desktop-handoff-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ env.LAB_DIR }}/desktop-public
          if-no-files-found: error
          retention-days: 1
      - name: Keep verified Gateway/Linux One Spot until lease expiry
        if: inputs.mode == 'apply'
        run: bash .github/scripts/xconnect-lab/run.sh node-observation

  existing_one:
    name: Existing UAT One / ${{ inputs.mode }}
    needs: preflight
    if: github.event_name == 'workflow_dispatch' && inputs.deployment_profile == 'existing-one' && inputs.mode != 'cleanup'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      XCONNECT_VAULT_ROLE: github-actions-platform-ops-toolkit-uat-xconnect-existing-one
      MODE: ${{ inputs.mode }}
      GITOPS_REF: ${{ inputs.gitops_ref || 'fbf78214efdc718802ffc9d170b457fa07fed8f0' }}
      PLAYBOOKS_REF: ${{ inputs.playbooks_ref || 'dae04558c6c3d86a9a12158cca6ef3b2fc9bc43f' }}
      CLI_RELEASE_TAG: ${{ inputs.cli_release_tag || 'v0.1.14' }}
      GATEWAY_RELEASE_TAG: ${{ inputs.gateway_release_tag || 'v0.1.10' }}
      XRAY_RELEASE_TAG: ${{ inputs.xray_release_tag || 'v26.3.27' }}
      ZERO_NETWORK_ID: ${{ inputs.network_id || 'net_uat' }}
      ONE_DEVICE_ID: observability-uat
      ONE_VAULT_KEY: ${{ inputs.one_vault_key || 'observability.svc.plus' }}
      GATEWAY_VAULT_KEY: ${{ inputs.gateway_vault_key || 'ph-xconnect.svc.plus' }}
      GATEWAY_SERVER_NAME: ph-xconnect.svc.plus
      GATEWAY_WIREGUARD_ADDRESS: ${{ inputs.gateway_wireguard_address }}
      SSH_DEBUG_INGRESS_CIDRS: ${{ inputs.ssh_debug_ingress_cidrs || '35.79.83.48/32' }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Validate existing-One inputs
        run: |
          [[ "${GITOPS_REF}" =~ ^[0-9a-f]{40}$ ]]
          [[ "${PLAYBOOKS_REF}" =~ ^[0-9a-f]{40}$ ]]
          [[ "${CLI_RELEASE_TAG}" =~ ^v[0-9A-Za-z._-]+$ ]]
          [[ "${GATEWAY_RELEASE_TAG}" =~ ^v[0-9A-Za-z._-]+$ ]]
          [[ "${XRAY_RELEASE_TAG}" =~ ^v[0-9A-Za-z._-]+$ ]]
          test "${SSH_DEBUG_INGRESS_CIDRS}" != '0.0.0.0/0'
          bash -n .github/scripts/xconnect-existing-one-uat/deploy.sh
      - name: Authenticate existing-One CI to Vault via GitHub JWT
        id: bootstrap
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ env.XCONNECT_VAULT_ROLE }}
          jwtGithubAudience: vault
          exportEnv: false
          secrets: |
            kv/data/CICD/github-app/daily-snapshot app_private_key | APP_PRIVATE_KEY
      - name: Create read-only infrastructure repository token
        id: infra-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: Iv23liNwStpQIiXajhpb
          private-key: ${{ steps.bootstrap.outputs.APP_PRIVATE_KEY }}
          owner: ai-workspace-infra
          repositories: gitops,playbooks
      - name: Create read-only XConnect release token
        id: artifact-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: Iv23liNwStpQIiXajhpb
          private-key: ${{ steps.bootstrap.outputs.APP_PRIVATE_KEY }}
          owner: ai-workspace-xstream
          repositories: XConnect-One,XConnect-Gateway
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ env.GITOPS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: gitops
          persist-credentials: false
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ env.PLAYBOOKS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: playbooks
          persist-credentials: false
      - name: Verify fixed UAT One declaration
        run: |
          declaration=gitops/vpn-overlay/uat/xconnect-one-nodes.yaml
          grep -Fq 'gateway_ref: ph-xconnect.svc.plus' "$declaration"
          grep -Fq 'fqdn: observability.svc.plus' "$declaration"
          grep -Fq 'lifecycle: persistent' "$declaration"
          grep -Fq 'deployment: enabled' "$declaration"
      - name: Read existing-One runtime credentials from Vault
        if: inputs.mode == 'apply'
        id: runtime
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ env.XCONNECT_VAULT_ROLE }}
          jwtGithubAudience: vault
          exportEnv: false
          secrets: |
            kv/data/uat/xconnect-one VLESS_ID | LAB_VLESS_ID ;
            kv/data/uat/xconnect-one ZERO_SERVICE_TOKEN | ZERO_SERVICE_TOKEN ;
            kv/data/uat/xconnect-one ZERO_OWNER_EMAIL | ZERO_OWNER_EMAIL ;
            kv/data/uat/ulighthost-xconnect/${{ env.ONE_VAULT_KEY }} host | ONE_HOST ;
            kv/data/uat/ulighthost-xconnect/${{ env.ONE_VAULT_KEY }} user | ONE_USER ;
            kv/data/uat/ulighthost-xconnect/${{ env.ONE_VAULT_KEY }} ssh_private_key_b64 | ONE_SSH_PRIVATE_KEY_B64 ;
            kv/data/uat/ulighthost-xconnect/${{ env.ONE_VAULT_KEY }} sudo_password | ONE_BECOME_PASSWORD ;
            kv/data/uat/ulighthost-xconnect/${{ env.GATEWAY_VAULT_KEY }} host | GATEWAY_HOST ;
            kv/data/uat/ulighthost-xconnect/${{ env.GATEWAY_VAULT_KEY }} user | GATEWAY_USER ;
            kv/data/uat/ulighthost-xconnect/${{ env.GATEWAY_VAULT_KEY }} ssh_private_key_b64 | GATEWAY_SSH_PRIVATE_KEY_B64 ;
            kv/data/CICD/domains/svc.plus tls_fullchain_pem_b64 | GATEWAY_TLS_CERT_B64 ;
            kv/data/CICD/domains/svc.plus tls_key_pem_b64 | GATEWAY_TLS_KEY_B64 ;
            kv/data/CICD/domains/svc.plus not_after_epoch | GATEWAY_TLS_NOT_AFTER_EPOCH ;
            kv/data/CICD/observability user | OBSERVABILITY_USER ;
            kv/data/CICD/observability password | OBSERVABILITY_PASSWORD
      - name: Install Ansible controller
        if: inputs.mode == 'apply'
        run: |
          python3 -m pip install --user 'ansible-core==2.17.7'
          echo "$(python3 -m site --user-base)/bin" >> "$GITHUB_PATH"
      - name: Enroll existing Linux One and verify UAT data plane
        if: inputs.mode == 'apply'
        env:
          LAB_DIR: ${{ runner.temp }}/xconnect-existing-one
          GITHUB_WORKSPACE: ${{ github.workspace }}
          CLI_RELEASE_TOKEN: ${{ steps.artifact-token.outputs.token }}
          GATEWAY_RELEASE_TAG: ${{ inputs.gateway_release_tag || 'v0.1.10' }}
          XRAY_RELEASE_TAG: ${{ inputs.xray_release_tag || 'v26.3.27' }}
          ZERO_ACCOUNTS_API_URL: https://accounts-uat.onwalk.net
          LAB_VLESS_ID: ${{ steps.runtime.outputs.LAB_VLESS_ID }}
          ZERO_SERVICE_TOKEN: ${{ steps.runtime.outputs.ZERO_SERVICE_TOKEN }}
          ZERO_OWNER_EMAIL: ${{ steps.runtime.outputs.ZERO_OWNER_EMAIL }}
          ONE_DEVICE_ID: observability-uat
          ONE_HOST: ${{ steps.runtime.outputs.ONE_HOST }}
          ONE_USER: ${{ steps.runtime.outputs.ONE_USER }}
          ONE_SSH_PRIVATE_KEY_B64: ${{ steps.runtime.outputs.ONE_SSH_PRIVATE_KEY_B64 }}
          ONE_BECOME_PASSWORD: ${{ steps.runtime.outputs.ONE_BECOME_PASSWORD }}
          GATEWAY_HOST: ${{ steps.runtime.outputs.GATEWAY_HOST }}
          GATEWAY_USER: ${{ steps.runtime.outputs.GATEWAY_USER }}
          GATEWAY_SSH_PRIVATE_KEY_B64: ${{ steps.runtime.outputs.GATEWAY_SSH_PRIVATE_KEY_B64 }}
          GATEWAY_TLS_CERT_B64: ${{ steps.runtime.outputs.GATEWAY_TLS_CERT_B64 }}
          GATEWAY_TLS_KEY_B64: ${{ steps.runtime.outputs.GATEWAY_TLS_KEY_B64 }}
          GATEWAY_TLS_NOT_AFTER_EPOCH: ${{ steps.runtime.outputs.GATEWAY_TLS_NOT_AFTER_EPOCH }}
          GATEWAY_SERVER_NAME: ph-xconnect.svc.plus
          OBSERVABILITY_USER: ${{ steps.runtime.outputs.OBSERVABILITY_USER }}
          OBSERVABILITY_PASSWORD: ${{ steps.runtime.outputs.OBSERVABILITY_PASSWORD }}
        run: bash .github/scripts/xconnect-existing-one-uat/deploy.sh

  enroll_node_matrix:
    name: Enroll One / ${{ matrix.node.id }} (${{ matrix.node.role }})
    needs: [preflight, apply, resolve_ai_aggregator_matrix]
    # `matrix` is only available after the matrix has been expanded; using it
    # in a job-level if makes GitHub reject the workflow before dispatch.
    # Selection is therefore evaluated by the first step below.
    if: github.event_name == 'workflow_dispatch' && inputs.deployment_profile == 'cloud-lab' && inputs.mode == 'apply'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      MODE: ${{ inputs.mode }}
      GITOPS_REF: ${{ inputs.gitops_ref || 'fbf78214efdc718802ffc9d170b457fa07fed8f0' }}
      PLAYBOOKS_REF: ${{ inputs.playbooks_ref || 'dae04558c6c3d86a9a12158cca6ef3b2fc9bc43f' }}
      CLI_RELEASE_TAG: ${{ inputs.cli_release_tag || 'v0.1.14' }}
      GATEWAY_PROVIDER: ${{ inputs.gateway_provider || 'external' }}
      EXTERNAL_GATEWAY_ID: ${{ inputs.external_gateway_id || 'gw-uat-tw-xconnect' }}
      EXTERNAL_NETWORK_ID: ${{ inputs.external_network_id || 'net_uat' }}
      EXTERNAL_GATEWAY_SERVER_NAME: ${{ inputs.external_gateway_server_name || 'tw-xconnect.svc.plus' }}
    strategy:
      fail-fast: false
      matrix:
        node: ${{ fromJSON(needs.resolve_ai_aggregator_matrix.outputs.matrix).node }}
    steps:
      - name: Select requested enrollment target
        id: selection
        env:
          MATRIX_NODE_FILTER: ${{ inputs.matrix_node_filter }}
          MATRIX_NODE_ID: ${{ matrix.node.id }}
          MATRIX_NODE_SUBSYSTEM: ${{ matrix.node.subsystem }}
        run: |
          selected=false
          if [[ "${MATRIX_NODE_FILTER}" == "all" ||
                "${MATRIX_NODE_FILTER}" == "${MATRIX_NODE_ID}" ||
                "${MATRIX_NODE_FILTER}" == "${MATRIX_NODE_SUBSYSTEM}" ]]; then
            selected=true
          fi
          echo "selected=${selected}" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v7
        if: steps.selection.outputs.selected == 'true'
        with:
          persist-credentials: false
      - name: Authenticate CI to Vault via GitHub JWT
        if: steps.selection.outputs.selected == 'true'
        id: bootstrap
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ env.XCONNECT_VAULT_ROLE }}
          jwtGithubAudience: vault
          exportEnv: false
          secrets: |
            kv/data/CICD/github-app/daily-snapshot app_private_key | APP_PRIVATE_KEY
      - name: Infra repository access
        if: steps.selection.outputs.selected == 'true'
        id: infra-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: Iv23liNwStpQIiXajhpb
          private-key: ${{ steps.bootstrap.outputs.APP_PRIVATE_KEY }}
          owner: ai-workspace-infra
          repositories: gitops,playbooks
      - name: CLI release token access
        if: steps.selection.outputs.selected == 'true'
        id: cli-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: Iv23liNwStpQIiXajhpb
          private-key: ${{ steps.bootstrap.outputs.APP_PRIVATE_KEY }}
          owner: ai-workspace-xstream
          repositories: XConnect-One
      - uses: actions/checkout@v7
        if: steps.selection.outputs.selected == 'true'
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ env.GITOPS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: gitops
          persist-credentials: false
      - uses: actions/checkout@v7
        if: steps.selection.outputs.selected == 'true'
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ env.PLAYBOOKS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: playbooks
          persist-credentials: false
      - name: Runtime credentials from dedicated Vault path
        if: steps.selection.outputs.selected == 'true'
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ env.XCONNECT_VAULT_ROLE }}
          jwtGithubAudience: vault
          secrets: |
            kv/data/uat/xconnect-one VLESS_ID | LAB_VLESS_ID ;
            kv/data/uat/xconnect-one ZERO_SERVICE_TOKEN | ZERO_SERVICE_TOKEN ;
            kv/data/uat/xconnect-one ZERO_OWNER_EMAIL | ZERO_OWNER_EMAIL ;
            kv/data/CICD/domains/svc.plus tls_ca_pem_b64 | GATEWAY_TLS_CA_PEM_B64 ;
            kv/data/CICD/domains/svc.plus tls_trust_bundle_pem_b64 | GATEWAY_TLS_TRUST_BUNDLE_PEM_B64
      - name: AWS short-lived OIDC credentials
        if: steps.selection.outputs.selected == 'true'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::950604983695:role/GithubAction_IAC_Deploy_Role
          aws-region: ap-northeast-1
          audience: sts.amazonaws.com
      - name: Download XConnect-One release
        if: steps.selection.outputs.selected == 'true'
        env:
          GH_TOKEN: ${{ steps.cli-token.outputs.token }}
        run: |
          mkdir -p "$RUNNER_TEMP/bin" "$RUNNER_TEMP/releases"
          gh release download "${{ env.CLI_RELEASE_TAG }}" \
            --repo ai-workspace-xstream/XConnect-One \
            --pattern 'xconnect-linux-arm64' \
            --pattern 'SHA256SUMS' --dir "$RUNNER_TEMP/releases" --clobber
          install -m 755 "$RUNNER_TEMP/releases/xconnect-linux-arm64" "$RUNNER_TEMP/bin/xconnect"
      - name: Enroll node into XConnect Zero
        if: steps.selection.outputs.selected == 'true'
        env:
          NODE_ID: ${{ matrix.node.id }}
          NODE_ROLE: ${{ matrix.node.role }}
          NODE_NAME: ${{ matrix.node.name }}
          NODE_OVERLAY_IP: ${{ matrix.node.overlay_ip }}
          LAB_DIR: ${{ runner.temp }}/xconnect-lab
          CLI_BINARY: ${{ runner.temp }}/bin/xconnect
        run: |
          bash .github/scripts/xconnect-lab/enroll-node.sh

  reconcile_mesh:
    name: Reconcile Gateway & Mesh
    needs: [apply, enroll_node_matrix]
    if: github.event_name == 'workflow_dispatch' && inputs.deployment_profile == 'cloud-lab' && inputs.mode == 'apply' && inputs.gateway_provider == 'external'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GATEWAY_PROVIDER: ${{ inputs.gateway_provider || 'external' }}
      EXTERNAL_GATEWAY_ID: ${{ inputs.external_gateway_id || 'gw-uat-tw-xconnect' }}
      EXTERNAL_NETWORK_ID: ${{ inputs.external_network_id || 'net_uat' }}
      EXTERNAL_GATEWAY_SERVER_NAME: ${{ inputs.external_gateway_server_name || 'tw-xconnect.svc.plus' }}
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Authenticate CI to Vault via GitHub JWT
        id: bootstrap
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ env.XCONNECT_VAULT_ROLE }}
          jwtGithubAudience: vault
          exportEnv: false
          secrets: |
            kv/data/prod/ulighthost-xconnect/tw-xconnect.svc.plus host | EXTERNAL_GATEWAY_HOST ;
            kv/data/prod/ulighthost-xconnect/tw-xconnect.svc.plus user | EXTERNAL_GATEWAY_USER ;
            kv/data/prod/ulighthost-xconnect/tw-xconnect.svc.plus ssh_private_key_b64 | EXTERNAL_GATEWAY_SSH_PRIVATE_KEY_B64
      - name: Reconcile Gateway Peer Table
        run: |
          echo "Reconciling Gateway with all matrix enrolled peers..."
          mkdir -p "$RUNNER_TEMP/gateway"
          printf '%s' "$EXTERNAL_GATEWAY_SSH_PRIVATE_KEY_B64" | base64 --decode > "$RUNNER_TEMP/gateway/key"
          chmod 600 "$RUNNER_TEMP/gateway/key"
          ssh -i "$RUNNER_TEMP/gateway/key" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new \
            "$EXTERNAL_GATEWAY_USER@$EXTERNAL_GATEWAY_HOST" \
            'sudo xconnect-gateway up --state-dir /var/lib/xconnect-gateway --tls-cert /etc/xconnect-gateway/tls.crt --tls-key /etc/xconnect-gateway/tls.key'
          rm -rf "$RUNNER_TEMP/gateway"
          echo "Gateway peer reconciliation complete."

  cleanup:
    name: UAT lab / explicit cleanup
    needs: preflight
    if: github.event_name == 'workflow_dispatch' && inputs.deployment_profile == 'cloud-lab' && inputs.mode == 'cleanup'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      MODE: cleanup
      IAC_REF: ${{ inputs.iac_ref || '2ba98f30c3e4199e01eb940bb1951549fca0ae3e' }}
      GITOPS_REF: ${{ inputs.gitops_ref || 'fbf78214efdc718802ffc9d170b457fa07fed8f0' }}
      PLAYBOOKS_REF: ${{ inputs.playbooks_ref || 'dae04558c6c3d86a9a12158cca6ef3b2fc9bc43f' }}
      CLI_RELEASE_TAG: ${{ inputs.cli_release_tag || 'v0.1.14' }}
      GATEWAY_RELEASE_TAG: ${{ inputs.gateway_release_tag || 'v0.1.10' }}
      XRAY_RELEASE_TAG: ${{ inputs.xray_release_tag || 'v26.3.27' }}
      GATEWAY_PROVIDER: ${{ inputs.gateway_provider || 'external' }}
      EXTERNAL_GATEWAY_ID: ${{ inputs.external_gateway_id || 'gw-uat-tw-xconnect' }}
      EXTERNAL_NETWORK_ID: ${{ inputs.external_network_id || 'net_uat' }}
      EXTERNAL_GATEWAY_SERVER_NAME: ${{ inputs.external_gateway_server_name || 'tw-xconnect.svc.plus' }}
      ALLOW_XCONNECT_RELEASE_OVERRIDES: ${{ inputs.allow_release_overrides || false }}
      CLEANUP_RUN: ${{ inputs.cleanup_run }}
      SSH_DEBUG_INGRESS_CIDRS: ${{ inputs.ssh_debug_ingress_cidrs || '35.79.83.48/32' }}
      GATEWAY_TRANSPORT_INGRESS_CIDRS: ${{ inputs.gateway_transport_ingress_cidrs || '35.79.83.48/32' }}
      GATEWAY_WIREGUARD_ADDRESS: ${{ inputs.gateway_wireguard_address }}
      NODE_OBSERVATION_INPUT: '0'
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Validate cleanup identity and immutable references
        run: |
          echo "LAB_DIR=$RUNNER_TEMP/xconnect-lab" >> "$GITHUB_ENV"
          LAB_DIR="$RUNNER_TEMP/xconnect-lab" bash .github/scripts/xconnect-lab/run.sh validate
      - name: Authenticate CI to Vault via GitHub JWT
        id: bootstrap
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ env.XCONNECT_VAULT_ROLE }}
          jwtGithubAudience: vault
          exportEnv: false
          secrets: |
            kv/data/CICD/github-app/daily-snapshot app_private_key | APP_PRIVATE_KEY
      - name: Infra repository access
        id: infra-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: Iv23liNwStpQIiXajhpb
          private-key: ${{ steps.bootstrap.outputs.APP_PRIVATE_KEY }}
          owner: ai-workspace-infra
          repositories: iac_modules,gitops,playbooks
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/iac_modules
          ref: ${{ env.IAC_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: iac_modules
          persist-credentials: false
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ env.GITOPS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: gitops
          persist-credentials: false
      - uses: actions/checkout@v7
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ env.PLAYBOOKS_REF }}
          token: ${{ steps.infra-token.outputs.token }}
          path: playbooks
          persist-credentials: false
      - name: Resolve the UAT declaration
        id: topology
        run: bash .github/scripts/xconnect-lab/run.sh topology
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.10.5
          terraform_wrapper: false
      - name: Infrastructure credentials from UAT Vault role
        id: state-credentials
        uses: hashicorp/vault-action@v4
        with:
          url: ${{ steps.topology.outputs.vault_address }}
          method: jwt
          role: ${{ steps.topology.outputs.vault_role }}
          jwtGithubAudience: vault
          secrets: |
            kv/data/CICD/uat/iac_state TF_STATE_ENDPOINT | TF_STATE_ENDPOINT ;
            kv/data/CICD/uat/iac_state TF_STATE_BUCKET | TF_STATE_BUCKET ;
            kv/data/CICD/uat/iac_state TF_STATE_ACCESS_KEY | TF_STATE_ACCESS_KEY ;
            kv/data/CICD/uat/iac_state TF_STATE_SECRET_KEY | TF_STATE_SECRET_KEY ;
            kv/data/CICD/uat/iac_state TF_STATE_REGION | TF_STATE_REGION
      - name: AWS short-lived OIDC credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.topology.outputs.aws_role }}
          aws-region: ${{ steps.topology.outputs.aws_region }}
          audience: sts.amazonaws.com
      - name: Re-open isolated state for explicit cleanup
        id: prepare
        run: bash .github/scripts/xconnect-lab/run.sh prepare
      - name: Refresh short-lived AWS OIDC credentials before explicit cleanup
        if: inputs.mode == 'cleanup' && steps.prepare.outcome == 'success'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.topology.outputs.aws_role }}
          aws-region: ${{ steps.topology.outputs.aws_region }}
          audience: sts.amazonaws.com
          unset-current-credentials: true
      - name: Destroy only the explicitly selected lab state
        if: inputs.mode == 'cleanup'
        run: bash .github/scripts/xconnect-lab/run.sh cleanup
