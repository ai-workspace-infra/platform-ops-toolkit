name: AI Aggregator v1

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/ai-aggregator-v1.yml'
      - 'scripts/ci/**'
      - 'scripts/verify_ai_aggregator_endpoints.sh'
      - 'scripts/vault/initialize_ai_aggregator_paths.sh'
      - 'docs/plans/**'
  pull_request:
    paths:
      - '.github/workflows/ai-aggregator-v1.yml'
      - 'scripts/ci/validate_ai_aggregator_manifest.py'
  workflow_dispatch:
    inputs:
      operation:
        description: "plan validates; provision creates ephemeral nodes; stage injects Gateway secrets; activate starts services"
        required: true
        default: plan
        type: choice
        options: [plan, provision, stage, activate]
      environment:
        description: "Target environment (uat: ai.onwalk.net, prod: ai.svc.plus)"
        required: true
        default: uat
        type: choice
        options: [uat, prod]
      gitops_ref:
        description: "Immutable GitOps ref"
        required: true
        default: main
      playbooks_ref:
        description: "Immutable Playbooks ref"
        required: true
        default: main
      target:
        description: "Inventory host to inspect or stage"
        required: true
        default: jp-xhttp-contabo.svc.plus
      provider:
        description: "Provider adapter; manifest keeps GitOps as the source of truth"
        required: true
        default: manifest
        type: choice
        options: [manifest, aws, gcp, vps, akamai-cloud]
      destroy:
        description: "Destroy ephemeral UAT resources when this dispatch finishes"
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  id-token: write

concurrency:
  group: ai-aggregator-v1-${{ inputs.environment || 'uat' }}-${{ inputs.target || github.head_ref || 'validation' }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install manifest parser
        run: pip install --quiet pyyaml
      - name: Checkout GitOps declaration
        uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
      - name: Validate GitOps declarations (UAT & PROD)
        run: |
          if [ -f gitops/topology/uat/selfhost/ai-aggregator.yaml ]; then
            python3 scripts/ci/validate_ai_aggregator_manifest.py gitops/topology/uat/selfhost/ai-aggregator.yaml
          fi
          if [ -f gitops/topology/prod/selfhost/ai-aggregator.yaml ]; then
            python3 scripts/ci/validate_ai_aggregator_manifest.py gitops/topology/prod/selfhost/ai-aggregator.yaml
          fi
      - name: Reject deprecated AI Aggregator Vault references
        run: |
          if rg -n --glob 'ai-aggregator.yaml' \
            'vault://kv/(uat|prod)/ai-aggregator/(accounts|instances|clients|cpa)/|vault://kv/(uat|prod)/ai-aggregator/database/postgresql|#(api_client_token|oauth_bundle|channel_token)' \
            gitops/topology; then
            echo 'deprecated AI Aggregator Vault reference detected' >&2
            exit 1
          fi

  resolve-provider:
    needs: validate
    if: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'push' }}
    runs-on: ubuntu-latest
    outputs:
      provider: ${{ steps.provider.outputs.provider }}
      environment: ${{ steps.provider.outputs.environment }}
      akamai_matrix: ${{ steps.provider.outputs.akamai_matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
      - name: Resolve provider from the selected environment declaration
        id: provider
        env:
          REQUESTED_PROVIDER: ${{ inputs.provider || 'manifest' }}
          TARGET_ENVIRONMENT: ${{ inputs.environment || 'uat' }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          import json
          from pathlib import Path
          import yaml

          environment = os.environ['TARGET_ENVIRONMENT']
          manifest_path = Path('gitops') / 'topology' / environment / 'selfhost' / 'ai-aggregator.yaml'
          data = yaml.safe_load(manifest_path.read_text())
          selected = data['spec']['infrastructure']['provider']
          adapter = 'vps' if selected in {'vps', 'vultr-vps'} else selected
          requested = os.environ['REQUESTED_PROVIDER']
          if requested != 'manifest' and requested != adapter:
              raise SystemExit(f'provider override {requested!r} does not match GitOps declaration {selected!r}')
          if selected not in {'aws', 'gcp', 'vps', 'vultr-vps', 'akamai-cloud', 'existing'}:
              raise SystemExit(f'unsupported provider adapter: {selected}')
          contract = data['spec']['infrastructure'].get('resource_contract', {})
          entries = []
          for raw_entry in contract.get('manifests', []) if adapter == 'akamai-cloud' else []:
              entry = dict(raw_entry)
              entry.setdefault('account', contract.get('account', ''))
              entries.append(entry)
          akamai_matrix = {'include': entries}
          if adapter == 'akamai-cloud' and not akamai_matrix['include']:
              raise SystemExit('Akamai provider requires resource_contract.manifests')
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'provider={adapter}\nenvironment={environment}\n')
              output.write('akamai_matrix<<AI_AGGREGATOR_MATRIX\n')
              output.write(json.dumps(akamai_matrix, separators=(',', ':')) + '\n')
              output.write('AI_AGGREGATOR_MATRIX\n')
          print(f'provider={adapter} environment={environment}')
  plan:
    needs: [validate, resolve-provider]
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ inputs.playbooks_ref || 'main' }}
          path: playbooks
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Ansible
        run: pip install --quiet ansible-core
      - name: Check deployment syntax
        working-directory: playbooks
        run: >-
          ansible-playbook --syntax-check -i localhost, deploy-ai-aggregator-v1.yml
          -e ai_aggregator_manifest_file=${{ github.workspace }}/gitops/topology/${{ inputs.environment }}/selfhost/ai-aggregator.yaml
      - name: Require an enabled declaration before remote staging
        if: ${{ inputs.operation == 'stage' || inputs.operation == 'activate' }}
        run: >-
          grep -q '^  enabled: true$' gitops/topology/${{ inputs.environment }}/selfhost/ai-aggregator.yaml

  uat-auto-deploy:
    needs: [validate, resolve-provider]
    if: >-
      ${{ needs.resolve-provider.outputs.provider == 'aws' &&
      ((github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.AI_AGGREGATOR_UAT_ENABLED == 'true') ||
      (github.event_name == 'workflow_dispatch' && inputs.environment == 'uat' &&
      contains(fromJSON('["provision","stage","activate"]'), inputs.operation))) }}
    runs-on: ubuntu-latest
    # Do not attach the GitHub Environment here. Its OIDC subject becomes
    # `repo:...:environment:uat`, while the least-privilege AWS role is
    # intentionally trusted only for the protected main branch ref.
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          path: gitops
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/playbooks
          path: playbooks
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/iac_modules
          path: iac_modules
          sparse-checkout: |
            terraform-hcl-standard
      - name: Set up Terraform and Python
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.10.5'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install render and Ansible dependencies
        run: pip install --quiet pyyaml jinja2 ansible-core
      - name: Resolve the UAT resource contract from GitOps
        id: contract
        env:
          GITOPS_MANIFEST: ${{ github.workspace }}/gitops/topology/uat/selfhost/ai-aggregator.yaml
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path
          import yaml

          manifest = yaml.safe_load(Path(os.environ['GITOPS_MANIFEST']).read_text())
          contract = manifest['spec']['infrastructure']['resource_contract']
          root = Path(os.environ['GITHUB_WORKSPACE']) / 'iac_modules'
          resources = root / contract['path']
          workdir = root / contract['workdir']
          if not resources.is_file():
              raise SystemExit(f'missing declared resource contract: {resources}')
          print(f'resources={resources}')
          print(f'workdir={workdir}')
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'resources={resources}\nworkdir={workdir}\n')
          PY
      - name: Configure AWS OIDC credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_IAC_ROLE_ARN }}
          aws-region: ap-northeast-1
      - name: Login to Vault with GitHub OIDC
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.operation == 'provision' || inputs.operation == 'stage' || inputs.operation == 'activate')) }}
        id: vault_login
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ vars.VAULT_JWT_ROLE }}
          jwtGithubAudience: vault
          exportToken: true
      - name: Render AWS UAT resources
        env:
          TARGET_DOMAIN_BASE: onwalk.net
          AI_AGGREGATOR_SSH_CIDR: ${{ vars.AI_AGGREGATOR_SSH_CIDR }}
          AI_AGGREGATOR_SOURCE_CIDR: ${{ vars.AI_AGGREGATOR_SOURCE_CIDR }}
          AI_AGGREGATOR_SSH_PUBLIC_KEY: ${{ vars.AI_AGGREGATOR_SSH_PUBLIC_KEY }}
        run: |
          python3 iac_modules/terraform-hcl-standard/aws-cloud/scripts/generate.py render \
            --resources "${{ steps.contract.outputs.resources }}" \
            --workdir "${{ steps.contract.outputs.workdir }}"
      - name: Provision AWS UAT Spot resources
        env:
          TARGET_DOMAIN_BASE: onwalk.net
          AI_AGGREGATOR_SSH_CIDR: ${{ vars.AI_AGGREGATOR_SSH_CIDR }}
          AI_AGGREGATOR_SOURCE_CIDR: ${{ vars.AI_AGGREGATOR_SOURCE_CIDR }}
          AI_AGGREGATOR_SSH_PUBLIC_KEY: ${{ vars.AI_AGGREGATOR_SSH_PUBLIC_KEY }}
        run: |
          terraform -chdir="${{ steps.contract.outputs.workdir }}" fmt -recursive
          # UAT is an ephemeral Spot test environment. Keep Terraform state on
          # the runner so no state bucket contract is required for this flow.
          rm -f "${{ steps.contract.outputs.workdir }}/backend.tf"
          terraform -chdir="${{ steps.contract.outputs.workdir }}" init -backend=false
          key_metadata="$(python3 - "${{ steps.contract.outputs.resources }}" <<'PY'
          import re
          import sys
          from pathlib import Path
          import yaml

          source = re.sub(r"\{\{.*?\}\}", "placeholder", Path(sys.argv[1]).read_text())
          data = yaml.safe_load(source)
          key_name = data['ssh_keys'][0]['name']
          tf_id = re.sub(r'[^0-9a-zA-Z_]', '_', str(key_name))
          print(f'{key_name}\t{tf_id}')
          PY
          )"
          IFS=$'\t' read -r key_name key_resource_id <<< "$key_metadata"
          if ! terraform -chdir="${{ steps.contract.outputs.workdir }}" state list | grep -Fxq "aws_key_pair.${key_resource_id}"; then
            terraform -chdir="${{ steps.contract.outputs.workdir }}" import -input=false \
              "aws_key_pair.${key_resource_id}" "$key_name" || \
              echo "No pre-existing contract SSH key found; Terraform will create it."
          fi
          terraform -chdir="${{ steps.contract.outputs.workdir }}" validate
          terraform -chdir="${{ steps.contract.outputs.workdir }}" apply -auto-approve
          python3 iac_modules/terraform-hcl-standard/aws-cloud/scripts/generate.py inventory \
            --resources "${{ steps.contract.outputs.resources }}" \
            --workdir "${{ steps.contract.outputs.workdir }}"
      - name: Deploy Gateway secrets and system configuration
        if: ${{ github.event_name == 'workflow_dispatch' && (inputs.operation == 'stage' || inputs.operation == 'activate') }}
        working-directory: playbooks
        env:
          VAULT_ADDR: https://vault.svc.plus
        run: |
          ansible-playbook -i ../iac_modules/terraform-hcl-standard/aws-cloud/envs/ai-aggregator-uat/inventory.ini \
            deploy-ai-aggregator-v1.yml \
            -e ai_aggregator_operation=${{ inputs.operation == 'activate' && 'activate' || 'stage' }} \
            -e ai_aggregator_manifest_file=${{ github.workspace }}/gitops/topology/uat/selfhost/ai-aggregator.yaml
      - name: Verify UAT endpoints
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.operation == 'activate' }}
        run: ./scripts/verify_ai_aggregator_endpoints.sh
      - name: Destroy UAT Spot resources
        if: ${{ always() && steps.contract.outputs.workdir != '' && (github.event_name != 'workflow_dispatch' || inputs.destroy) }}
        run: terraform -chdir="${{ steps.contract.outputs.workdir }}" destroy -auto-approve

  uat-gcp-auto-deploy:
    needs: [validate, resolve-provider]
    if: >-
      ${{ needs.resolve-provider.outputs.provider == 'gcp' &&
      ((github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.AI_AGGREGATOR_UAT_ENABLED == 'true') ||
      (github.event_name == 'workflow_dispatch' && inputs.environment == 'uat' &&
      contains(fromJSON('["provision","stage","activate"]'), inputs.operation))) }}
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ inputs.playbooks_ref || 'main' }}
          path: playbooks
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/iac_modules
          ref: main
          path: iac_modules
          sparse-checkout: |
            terraform-hcl-standard
      - name: Set up Terraform and Python
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.10.5'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install render and Ansible dependencies
        run: pip install --quiet pyyaml jinja2 ansible-core
      - name: Resolve the GCP UAT resource contract from GitOps
        id: contract
        env:
          GITOPS_MANIFEST: ${{ github.workspace }}/gitops/topology/uat/selfhost/ai-aggregator.yaml
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path
          import yaml

          manifest = yaml.safe_load(Path(os.environ['GITOPS_MANIFEST']).read_text())
          infrastructure = manifest['spec']['infrastructure']
          if infrastructure['provider'] != 'gcp':
              raise SystemExit('GCP adapter selected but GitOps provider is not gcp')
          root = Path(os.environ['GITHUB_WORKSPACE']) / 'iac_modules'
          resources = root / infrastructure['resource_contract']['path']
          workdir = root / infrastructure['resource_contract']['workdir']
          if not resources.is_file():
              raise SystemExit(f'missing declared resource contract: {resources}')
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'resources={resources}\nworkdir={workdir}\n')
          PY
      - name: Login to Vault with GitHub OIDC
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.operation == 'provision' || inputs.operation == 'stage' || inputs.operation == 'activate')) }}
        id: vault_login
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ vars.VAULT_GCP_JWT_ROLE }}
          jwtGithubAudience: vault
          exportToken: true
      - name: Provision GCP UAT Spot instance
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.operation == 'provision' || inputs.operation == 'stage' || inputs.operation == 'activate')) }}
        env:
          VAULT_ADDR: https://vault.svc.plus
          VAULT_TOKEN: ${{ steps.vault_login.outputs.token }}
          AI_AGGREGATOR_SSH_CIDR: ${{ vars.AI_AGGREGATOR_SSH_CIDR }}
          AI_AGGREGATOR_SOURCE_CIDR: ${{ vars.AI_AGGREGATOR_SOURCE_CIDR }}
          AI_AGGREGATOR_SSH_PUBLIC_KEY: ${{ vars.AI_AGGREGATOR_SSH_PUBLIC_KEY }}
          TARGET_DOMAIN_BASE: onwalk.net
        run: |
          terraform -chdir="${{ steps.contract.outputs.workdir }}" init -backend=false -input=false
          terraform -chdir="${{ steps.contract.outputs.workdir }}" validate
          terraform -chdir="${{ steps.contract.outputs.workdir }}" apply -auto-approve
      - name: Deploy GCP UAT services
        if: ${{ github.event_name == 'workflow_dispatch' && (inputs.operation == 'stage' || inputs.operation == 'activate') }}
        working-directory: playbooks
        env:
          VAULT_ADDR: https://vault.svc.plus
        run: |
          set -euo pipefail
          ansible-playbook -i ../iac_modules/terraform-hcl-standard/gcp-cloud/envs/ai-aggregator-uat/inventory.ini             deploy-ai-aggregator-v1.yml             -e ai_aggregator_operation=stage             -e ai_aggregator_manifest_file=${{ github.workspace }}/gitops/topology/uat/selfhost/ai-aggregator.yaml
          desktop_vars="$RUNNER_TEMP/ai-desktop-vars.json"
          trap 'rm -f "$desktop_vars"' EXIT
          jq -n '{ai_desktop_password_by_host: {}}' > "$desktop_vars"
          while IFS= read -r cpa_id; do
            [ -n "$cpa_id" ] || continue
            password="$(curl --fail --silent --show-error               -H "X-Vault-Token: $VAULT_TOKEN"               "$VAULT_ADDR/v1/kv/data/uat/ai-aggregator/cpa/$cpa_id" |               jq -er '.data.data.desktop_password')"
            jq --arg id "$cpa_id" --arg password "$password"               '.ai_desktop_password_by_host[$id] = $password' "$desktop_vars" > "$desktop_vars.next"
            mv "$desktop_vars.next" "$desktop_vars"
          done < <(python3 - <<'PY'
          import yaml
          data = yaml.safe_load(open('../gitops/topology/uat/selfhost/ai-aggregator.yaml'))
          for entry in data['spec']['cpa_instances']:
              print(entry['node'])
          PY
          )
          ansible-playbook -i ../iac_modules/terraform-hcl-standard/gcp-cloud/envs/ai-aggregator-uat/inventory.ini             deploy_ai_desktop.yml             -e ai_desktop_remote_enabled=true             -e ai_desktop_manage_user=true             -e @"$desktop_vars"
      - name: Verify UAT endpoints
        if: ${{ github.event_name == 'workflow_dispatch' && (inputs.operation == 'stage' || inputs.operation == 'activate') }}
        env:
          AI_AGGREGATOR_MANIFEST: gitops/topology/uat/selfhost/ai-aggregator.yaml
        run: ./scripts/verify_ai_aggregator_endpoints.sh
      - name: Destroy GCP UAT Spot resources
        if: ${{ always() && steps.contract.outputs.workdir != '' && (github.event_name != 'workflow_dispatch' || inputs.destroy) }}
        run: terraform -chdir="${{ steps.contract.outputs.workdir }}" destroy -auto-approve

  uat-vps-auto-deploy:
    needs: [validate, resolve-provider]
    if: >-
      ${{ needs.resolve-provider.outputs.provider == 'vps' &&
      ((github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.AI_AGGREGATOR_UAT_ENABLED == 'true') ||
      (github.event_name == 'workflow_dispatch' && inputs.environment == 'uat' &&
      contains(fromJSON('["provision","stage","activate"]'), inputs.operation))) }}
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ inputs.playbooks_ref || 'main' }}
          path: playbooks
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/iac_modules
          ref: main
          path: iac_modules
          sparse-checkout: |
            terraform-hcl-standard
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.10.5'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install render and Ansible dependencies
        run: pip install --quiet pyyaml jinja2 ansible-core
      - name: Resolve the VPS UAT resource contract from GitOps
        id: contract
        env:
          GITOPS_MANIFEST: ${{ github.workspace }}/gitops/topology/uat/selfhost/ai-aggregator.yaml
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path
          import yaml

          manifest = yaml.safe_load(Path(os.environ['GITOPS_MANIFEST']).read_text())
          infrastructure = manifest['spec']['infrastructure']
          if infrastructure['provider'] != 'vps':
              raise SystemExit('VPS adapter selected but GitOps provider is not vps')
          root = Path(os.environ['GITHUB_WORKSPACE']) / 'iac_modules'
          contract = infrastructure['resource_contract']
          resources = root / contract['path']
          workdir = root / contract['workdir']
          if not resources.is_file():
              raise SystemExit(f'missing declared resource contract: {resources}')
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f"resources={resources}\nworkdir={workdir}\nrenderer={root / contract['renderer']}\n")
          PY
      - name: Login to Vault with GitHub OIDC
        id: vault_login
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ vars.VAULT_VPS_JWT_ROLE }}
          jwtGithubAudience: vault
          exportToken: true
      - name: Load VPS provider credential from Vault
        id: vps_vault
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          path: jwt
          role: ${{ vars.VAULT_VPS_JWT_ROLE }}
          jwtGithubAudience: vault
          secrets: |
            kv/data/uat/ai-aggregator/gateway/vps VULTR_API_KEY | VULTR_API_KEY
      - name: Render and provision the selected VPS resources
        env:
          VULTR_API_KEY: ${{ steps.vps_vault.outputs.VULTR_API_KEY }}
          AI_AGGREGATOR_SSH_CIDR: ${{ vars.AI_AGGREGATOR_SSH_CIDR }}
          AI_AGGREGATOR_SOURCE_CIDR: ${{ vars.AI_AGGREGATOR_SOURCE_CIDR }}
          AI_AGGREGATOR_SSH_PUBLIC_KEY: ${{ vars.AI_AGGREGATOR_SSH_PUBLIC_KEY }}
          TARGET_DOMAIN_BASE: onwalk.net
        run: |
          python3 "${{ steps.contract.outputs.renderer }}" render             --resources "${{ steps.contract.outputs.resources }}"             --workdir "${{ steps.contract.outputs.workdir }}"
          terraform -chdir="${{ steps.contract.outputs.workdir }}" fmt -recursive
          terraform -chdir="${{ steps.contract.outputs.workdir }}" init -backend=false -input=false
          terraform -chdir="${{ steps.contract.outputs.workdir }}" validate
          terraform -chdir="${{ steps.contract.outputs.workdir }}" apply -auto-approve
          python3 "${{ steps.contract.outputs.renderer }}" inventory             --resources "${{ steps.contract.outputs.resources }}"             --workdir "${{ steps.contract.outputs.workdir }}"
      - name: Deploy selected VPS services
        if: ${{ github.event_name == 'workflow_dispatch' && (inputs.operation == 'stage' || inputs.operation == 'activate') }}
        working-directory: playbooks
        env:
          VAULT_ADDR: https://vault.svc.plus
        run: |
          ansible-playbook -i "${{ steps.contract.outputs.workdir }}/inventory.ini"             deploy-ai-aggregator-v1.yml             -e ai_aggregator_operation=stage             -e ai_aggregator_manifest_file=${{ github.workspace }}/gitops/topology/uat/selfhost/ai-aggregator.yaml
      - name: Verify selected VPS endpoints
        if: ${{ github.event_name == 'workflow_dispatch' && (inputs.operation == 'stage' || inputs.operation == 'activate') }}
        env:
          AI_AGGREGATOR_MANIFEST: gitops/topology/uat/selfhost/ai-aggregator.yaml
        run: ./scripts/verify_ai_aggregator_endpoints.sh
      - name: Destroy VPS UAT resources
        if: ${{ always() && steps.contract.outputs.workdir != '' && (github.event_name != 'workflow_dispatch' || inputs.destroy) }}
        env:
          VULTR_API_KEY: ${{ steps.vps_vault.outputs.VULTR_API_KEY }}
        run: terraform -chdir="${{ steps.contract.outputs.workdir }}" destroy -auto-approve

  uat-akamai-auto-deploy:
    needs: [validate, resolve-provider]
    if: >-
      ${{ needs.resolve-provider.outputs.provider == 'akamai-cloud' &&
      github.event_name == 'workflow_dispatch' && inputs.environment == 'uat' &&
      contains(fromJSON('["provision","stage","activate"]'), inputs.operation) }}
    runs-on: ubuntu-latest
    environment: uat
    timeout-minutes: 75
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.resolve-provider.outputs.akamai_matrix) }}
    env:
      AKAMAI_ACCOUNT: ${{ matrix.account }}
      AKAMAI_WORKSPACE: ${{ matrix.workspace }}
      AKAMAI_MANIFEST: ${{ matrix.path }}
      TF_ROOT: ${{ github.workspace }}/iac_modules/terraform-hcl-standard/akamai-cloud/envs/uat/${{ matrix.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/iac_modules
          ref: main
          path: iac_modules
          sparse-checkout: |
            terraform-hcl-standard
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ inputs.playbooks_ref || 'main' }}
          path: playbooks
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.10.5'
      - name: Install Akamai render and Ansible dependencies
        run: pip install --quiet pyyaml jinja2 ansible-core
      - name: Login to Vault with GitHub OIDC
        id: vault
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: github-actions-platform-ops-toolkit-uat-akamai-oidc-bootstrap-${{ env.AKAMAI_ACCOUNT }}
          jwtGithubAudience: vault
          exportToken: false
          secrets: |
            kv/data/CICD/uat/akamai-cloud/${{ env.AKAMAI_ACCOUNT }} LINODE_TOKEN | LINODE_TOKEN ;
            kv/data/CICD/uat/iac_state TF_STATE_ENDPOINT | TF_STATE_ENDPOINT ;
            kv/data/CICD/uat/iac_state TF_STATE_BUCKET | TF_STATE_BUCKET ;
            kv/data/CICD/uat/iac_state TF_STATE_ACCESS_KEY | TF_STATE_ACCESS_KEY ;
            kv/data/CICD/uat/iac_state TF_STATE_SECRET_KEY | TF_STATE_SECRET_KEY ;
            kv/data/CICD/uat/iac_state TF_STATE_REGION | TF_STATE_REGION
      - name: Validate Akamai provider token
        env:
          LINODE_TOKEN: ${{ steps.vault.outputs.LINODE_TOKEN }}
        run: |
          set -euo pipefail
          token="$(printf '%s' "$LINODE_TOKEN" | tr -d '\r\n')"
          test -n "$token"
          echo "::add-mask::$token"
          printf 'LINODE_TOKEN=%s\n' "$token" >> "$GITHUB_ENV"
          printf 'TF_VAR_linode_token=%s\n' "$token" >> "$GITHUB_ENV"
          curl --fail --silent --show-error --header "Authorization: Bearer $token" \
            https://api.linode.com/v4/profile | jq -e '.username and .email' >/dev/null
      - name: Resolve and validate Akamai manifest
        run: |
          set -euo pipefail
          case "$AKAMAI_MANIFEST" in
            resources/svc.plus/uat/akamai/*.yaml) ;;
            *) echo "invalid Akamai resource manifest path" >&2; exit 1 ;;
          esac
          test -f "gitops/$AKAMAI_MANIFEST"
          mkdir -p "$TF_ROOT"
      - name: Render Akamai Terraform root
        env:
          GITOPS_ROOT: ${{ github.workspace }}/gitops
          TF_STATE_ENDPOINT: ${{ steps.vault.outputs.TF_STATE_ENDPOINT }}
          TF_STATE_REGION: ${{ steps.vault.outputs.TF_STATE_REGION }}
        run: |
          python3 iac_modules/terraform-hcl-standard/akamai-cloud/scripts/generate.py render \
            --resources "${GITHUB_WORKSPACE}/gitops/${AKAMAI_MANIFEST}" \
            --workdir "$TF_ROOT" --namespace "$AKAMAI_WORKSPACE"
      - name: Configure Akamai state backend
        env:
          TF_STATE_ENDPOINT: ${{ steps.vault.outputs.TF_STATE_ENDPOINT }}
          TF_STATE_BUCKET: ${{ steps.vault.outputs.TF_STATE_BUCKET }}
          TF_STATE_ACCESS_KEY: ${{ steps.vault.outputs.TF_STATE_ACCESS_KEY }}
          TF_STATE_SECRET_KEY: ${{ steps.vault.outputs.TF_STATE_SECRET_KEY }}
          TF_STATE_REGION: ${{ steps.vault.outputs.TF_STATE_REGION }}
        run: |
          set -euo pipefail
          umask 077
          backend="$RUNNER_TEMP/akamai-backend.hcl"
          printf '%s\n' \
            "bucket = \"$TF_STATE_BUCKET\"" \
            "key = \"terraform/uat/svc.plus/akamai-cloud/$AKAMAI_ACCOUNT/$AKAMAI_WORKSPACE/terraform.tfstate\"" \
            "region = \"$TF_STATE_REGION\"" \
            "access_key = \"$TF_STATE_ACCESS_KEY\"" \
            "secret_key = \"$TF_STATE_SECRET_KEY\"" \
            'skip_credentials_validation = true' \
            'skip_region_validation = true' \
            'skip_requesting_account_id = true' \
            'skip_metadata_api_check = true' \
            'skip_s3_checksum = true' \
            'use_path_style = true' \
            'use_lockfile = true' > "$backend"
          terraform -chdir="$TF_ROOT" init -input=false -reconfigure -backend-config="$backend"
      - name: Provision Akamai UAT resource
        run: |
          set -euo pipefail
          terraform -chdir="$TF_ROOT" validate -no-color
          terraform -chdir="$TF_ROOT" plan -input=false -no-color
          terraform -chdir="$TF_ROOT" apply -input=false -auto-approve
          python3 iac_modules/terraform-hcl-standard/akamai-cloud/scripts/generate.py inventory \
            --resources "${GITHUB_WORKSPACE}/gitops/${AKAMAI_MANIFEST}" \
            --workdir "$TF_ROOT" --namespace "$AKAMAI_WORKSPACE"
      - name: Deploy AI Desktop CodeAgent role on CPA node
        if: ${{ (inputs.operation == 'stage' || inputs.operation == 'activate') && matrix.role == 'cpa' }}
        working-directory: playbooks
        run: |
          ansible-playbook -i "$TF_ROOT/inventory.ini" deploy_ai_desktop.yml \
            --limit ai_aggregator_cpa \
            -e ai_desktop_cpa_codeagent=true \
            -e ai_desktop_remote_enabled=false \
            -e ai_desktop_manage_user=false
      - name: Stage or activate aggregator service on selected node
        if: ${{ inputs.operation == 'stage' || inputs.operation == 'activate' }}
        working-directory: playbooks
        env:
          VAULT_ADDR: https://vault.svc.plus
        run: |
          ansible-playbook -i "$TF_ROOT/inventory.ini" deploy-ai-aggregator-v1.yml \
            -e ai_aggregator_operation=${{ inputs.operation }} \
            -e ai_aggregator_manifest_file=${{ github.workspace }}/gitops/topology/uat/selfhost/ai-aggregator.yaml
      - name: Destroy Akamai UAT resource
        if: ${{ always() && (github.event_name != 'workflow_dispatch' || inputs.destroy) }}
        run: terraform -chdir="$TF_ROOT" destroy -input=false -auto-approve

  prod-deploy:
    needs: [validate, resolve-provider]
    if: >-
      ${{ github.event_name == 'workflow_dispatch' && inputs.environment == 'prod' &&
      (inputs.operation == 'stage' || inputs.operation == 'activate') }}
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/gitops
          ref: ${{ inputs.gitops_ref || 'main' }}
          path: gitops
      - uses: actions/checkout@v4
        with:
          repository: ai-workspace-infra/playbooks
          ref: ${{ inputs.playbooks_ref || 'main' }}
          path: playbooks
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Ansible dependencies
        run: pip install --quiet ansible-core pyyaml
      - name: Resolve declared Prod target hosts
        id: target_hosts
        env:
          GITOPS_MANIFEST: ${{ github.workspace }}/gitops/topology/prod/selfhost/ai-aggregator.yaml
        run: |
          python3 - <<'PY'
          import os
          from pathlib import Path
          import yaml

          manifest = yaml.safe_load(Path(os.environ['GITOPS_MANIFEST']).read_text())
          hosts = sorted({node['inventory_host'] for node in manifest['spec']['nodes']})
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f"hosts={','.join(hosts)}\n")
          PY
      - name: Login to Vault with GitHub OIDC
        id: vault_login
        uses: hashicorp/vault-action@v4
        with:
          url: https://vault.svc.plus
          method: jwt
          role: ${{ vars.VAULT_JWT_ROLE }}
          jwtGithubAudience: vault
          exportToken: true
      - name: Deploy Prod to existing persistent nodes
        working-directory: playbooks
        env:
          VAULT_ADDR: https://vault.svc.plus
        run: |
          ansible-playbook -i inventory.ini --limit "${{ steps.target_hosts.outputs.hosts }}" deploy-ai-aggregator-v1.yml             -e ai_aggregator_operation=${{ inputs.operation }}             -e ai_aggregator_manifest_file=${{ github.workspace }}/gitops/topology/prod/selfhost/ai-aggregator.yaml
      - name: Verify Prod endpoints
        if: ${{ inputs.operation == 'activate' }}
        env:
          AI_AGGREGATOR_MANIFEST: ${{ github.workspace }}/gitops/topology/prod/selfhost/ai-aggregator.yaml
        run: ./scripts/verify_ai_aggregator_endpoints.sh
